#include "main.h"

/**
 * main - prints the alphabet in lowercase
 *
 * Return: Always 0.
 */
int main(void)
{
	char alphabet;

	while (alphabet <= 'z')
		_putchar(alphabet);

	_putchar('\n');

	return (0);
}
